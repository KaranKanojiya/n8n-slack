# local-n8n-setup

Docker Compose and config for n8n + Postgres + ngrok (Slack Trigger).

**Full documentation:** [README in repository root](../README.md) — setup, Slack, ngrok, troubleshooting, backup/restore.

Run all commands from this directory (`local-n8n-setup`), e.g.:

```bash
docker compose up -d
docker compose down
./backup-volumes.sh
```
